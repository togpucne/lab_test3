<%- include('../partials/header') %>

<div class="row justify-content-center mt-5">
  <div class="col-md-5">
    <div class="card shadow-lg rounded-3">
      <div class="card-body">
        <h3 class="card-title text-center mb-4">🔄 Quên mật khẩu</h3>

        <% if (info) { %>
          <div class="alert alert-success text-center"><%= info %></div>
        <% } %>

        <form method="POST" action="/auth/forgot">
          <div class="mb-3">
            <input type="email" name="email" class="form-control" placeholder="Nhập email" required />
          </div>
          <div class="d-grid">
            <button type="submit" class="btn btn-warning">Gửi yêu cầu đặt lại</button>
          </div>
        </form>

        <div class="mt-3 text-center">
          <a href="/auth/login">Quay lại đăng nhập</a>
        </div>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
